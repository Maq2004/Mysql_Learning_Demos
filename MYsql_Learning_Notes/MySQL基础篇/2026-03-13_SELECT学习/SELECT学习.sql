-- 1.DDL是数据定义，DML是数据操作，DCL是数据控制语言，最为重要的是DML是SQL语言的基础，因为查询语句使用频繁，所以被单拎出来乘坐数据查询语言

-- 2.在Windows中大小写不敏感，在Linux中大小写不敏感，推荐的书写规范是数据库名表名表别名字段名字段别名小写，SQL关键字，函数名，绑定变量大写
-- 
-- 3.每条SQL语句都要以; \g \G结束，关键词不能分行和缩写，标点符号的要求是英文半角输入
-- 
-- 4.起别名的时候AS都可以省略，别名中有空格的话不能省略双引号，因为用的话没坏处，不用的话字段别名中有空格会报错
-- 
-- 5.必须放到所有列名前面，如果写成后面那样会直接报错
-- 
-- 6.NULL和空字符串是不一样的，一个空字符串的长度是零，而一个空值的长度是空，在MySQL 里空值是占用空间的
-- 
-- 7.在和保留字，数据库系统和常用方法冲突的时候，需要用着重号''引起来
-- 
-- 8.可以查询常数，如果你需要整合不同的数据源可以用这个常数列做标记
-- 
-- 9.用于显示表结构，field:字段名称，Type:字段类型,Null:是否可以存储NUll值，
-- key：是否已编制索引，
-- PRI表示该列是表主键的一部分，
-- UNI表示该列是UNIQUE部分,
-- Default：表示该列是否有默认值，
-- Extra：可以获取的与给定列有关的附加信息
-- 
-- 10.where必须紧跟在from后面，用于过滤条件 SELECT salary FROM employee WHERE salary = 300;
-- 

-- 1.SELECT * 是返回所有字段的信息，在生产环境中数据多，所以不建议使用
-- 2.会得到去重重复行的数据，因为DISTINCT是去重关键词
-- 3.查询常数可以在整合其他数据的时候用这个常数列进行标记，排列 SELECT "尚硅谷" AS corporation ,last_name FROM employees
-- 4.PRI是主键，UNI是UNIQUE
-- 5.还可以过滤字符串数据 SELECT employee_id,job_id FROM employees WHERE job_id = "AD_VP"